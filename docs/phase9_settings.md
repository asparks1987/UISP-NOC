# Phase 9 Item 7 - Settings (Android)

## Features
- Site selection, quiet hours, channel preferences, API/UISP URLs, debug toggles.
- Show push token status and last sync.

## Error/Diagnostics
- Save failures show banner with request_id and hints (invalid URL, missing scope).
